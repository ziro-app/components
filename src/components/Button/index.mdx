---
name: Button
menu: Componentes
route: /button
---

import { Playground, Props } from 'docz'
import Button from './index'
import { containerDocz } from '../../stylesDocz'
import { fontTitle, fontSizeInput, primaryColor } from '@ziro/theme'

# Button
Cria um botão genérico que pode ser usado em forms (submit), como navegação (link) e para chamar eventos (click).

<Playground>
	<div style={{ ...containerDocz, display: 'grid', gridTemplateColumns: 'auto auto auto', gridColumnGap: '30px', gridRowGap: '20px', margin: '0 auto 20px', textAlign: 'center' }}>
		<Button type='submit' cta='Submit' template='regular' />
		<Button type='button' cta='Click' template='destructive' click={() => null} />
		<Button type='link' cta='Link' template='light' navigate={() => null} />
		<Button type='submit' cta='Disabled' submitting={true} template='light' />
		<Button type='submit' cta='Custom' template='light' style={{
			display: 'block',
			WebkitAppearance: 'none',
			WebkitTapHighlightColor: 'rgba(0,0,0,0)',
			MozAppearance: 'none',
			outline: 'none',
			cursor: 'pointer',
			width: '100%',
			padding: '10px 0px',
			border: `2px solid ${primaryColor}`,
			borderRadius: '20px',
			fontFamily: fontTitle,
			fontSize: fontSizeInput,
			color: primaryColor,
			textAlign: 'center',
			background: 'white'
		}} />
	</div>
</Playground>

<Props of={Button} />