---
name: CameraButton
menu: Componentes
---

import { Playground, Props } from 'docz'
import CameraButton from './index'
import { containerDocz } from '../../stylesDocz'

# Button
Cria um botão de camera que pode ser *shooter*, *toggle* ou *video*

<Playground>
	<div style={{ ...containerDocz, display: 'grid', gridTemplateColumns: 'auto auto auto', gridColumnGap: '30px', margin: '0 auto 20px', textAlign: 'center' }}>
		<CameraButton type='shooter' />
		<CameraButton type='toggle' />
		<CameraButton type='video' position='on'/>
	</div>
</Playground>

<Props of={Button} />