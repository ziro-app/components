import { fontTitle } from '@ziro/theme'

export const

container = {
    display: 'grid',
    padding: '10px'
},

button = {
    height: '40px',
    display: 'grid',
    alignItems: 'center',
    justifyItems: 'center'
}