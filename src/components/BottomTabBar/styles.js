import { fontTitle } from '@ziro/theme'

export const

container = {
    display: 'grid',
    padding: '10px'
},

button = {
    height: '40px',
    display: 'grid'
},

iconContainer = {
    display: 'grid',
    alignItems: 'center',
    justifyItems: 'center'
},

titleContainer = {
    overflow: 'hidden',
    alignItems: 'center',
    justifyItems: 'center',
    display: 'grid'
},

 titleFont = {
    fontFamily: fontTitle,
    fontSize: 12
}