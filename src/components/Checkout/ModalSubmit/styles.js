import { fontTitle, primaryColor, alertColor } from '@ziro/theme'

export const

container = {
	display: 'grid',
	gridRowGap: '15px',
	color: primaryColor
},

svg = {
	justifySelf: 'center'
},

title = {
	fontFamily: fontTitle,
	textTransform: 'uppercase',
	color: alertColor
}