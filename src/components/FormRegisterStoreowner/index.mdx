---
name: FormRegisterStoreowner
menu: Componentes
route: /form-register-storeowner
---

import { Playground, Props } from 'docz'
import { useState } from 'react'
import FormRegisterStoreowner from './index'
import { containerDocz } from '../../stylesDocz'

# FormRegisterStoreowner
Componente padrão utilizado para adicionar um novo lojista.

<Playground>
    {() => {
        const [isLoading, setIsLoading] = useState(false)
        const validCnaes = ['47.81-4-00', '14.12-6-01', '14.12-6-03'];
        // Usar as env's
        const cnpjUrl = '';
        const cnpjToken = '';
        return(
            <div style={containerDocz}>
                <FormRegisterStoreowner isLoading={isLoading} setIsLoading={setIsLoading} sendToBackend={() => () => console.log('sendToBackend')} fetch={() => console.log('Your fetch function')} hasAdvisor={true} hasAffiliated={true} haveSalesman={true} validCnaes={validCnaes} cnpjUrl={cnpjUrl} cnpjToken={cnpjToken} />
            </div>
        )
    }}
</Playground>