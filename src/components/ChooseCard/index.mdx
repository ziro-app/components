---
name: ChooseCard
menu: Componentes
route: /choose-card
---

import { useState } from 'react'
import { Playground, Props } from 'docz'
import ChooseCard from './index'
import { containerDocz } from '../../stylesDocz'

# ChooseCard
Componente que exibe diversos números de cartão para que o usuário escolha qual será utilizado, inclui um botão para adcionar novo cartão

<Playground>
	{(() => {
		const [index, setIndex] = useState()
		return (
			<ChooseCard
			/>
		)
	})()}
</Playground>

<Props of={ChooseCard} />