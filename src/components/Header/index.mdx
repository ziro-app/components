---
name: Header
menu: Components
---

import { Playground, Props } from 'docz'
import { useState } from 'react'
import Header from './index'
import { containerWithoutHeight } from '../../styles/index'

# Header
Componente de cabeçalho, responsável por apresentar alguma mensagem padrão, ícones e link's para o usuário.

<Playground>
	<div style={{height: 200, ...containerWithoutHeight}}>
		<Header type='title-only' title='Header Title Example' />
		<Header type='icon' title='Header Icon Example' icon='menu' setIsOpen={() => null} />
		<Header type='icon-link' title='Header Link Example' icon='back' />
	</div>
</Playground>

<Props of={Header} />