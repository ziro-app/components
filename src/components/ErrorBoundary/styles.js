import { primaryColor, alertColor, fontTitle, fontWeightTitle } from '@ziro/theme'

export const 

container = {
	textAlign: 'center',
	color: alertColor
},

title = {
	color: primaryColor,
	fontFamily: fontTitle,
	fontWeight: fontWeightTitle,
	textTransform: 'uppercase'
},

alert = {
	display: 'grid',
	alignItems: 'center',
	justifyItems: 'center'
}