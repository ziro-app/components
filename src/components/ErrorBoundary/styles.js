import { primaryColor, alertColor, fontTitle, fontWeightTitle } from '../../Theme/variables'

export const 

container = {
	textAlign: 'center',
	color: alertColor
},

title = {
	color: primaryColor,
	fontFamily: fontTitle,
	fontWeight: fontWeightTitle,
	textTransform: 'uppercase'
},

alert = {
	display: 'grid',
	alignItems: 'center',
	justifyItems: 'center'
}