export const

container = (background) => ({
    display: 'flex',
    position: 'fixed',
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    background
}),

overlay = {
    position: 'fixed',
    width: '100%',
    height: '100%'
}