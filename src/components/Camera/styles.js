export const

container = (background) => ({
    display: 'grid',
    width: '100%',
    height: '100vh',
    justifyContent: 'center',
    alignItems: 'center',
    overflow: 'hidden',
    background
}),

overlay = {
    display: 'grid',
    width: '100%',
    height: '100vh',
    justifyContent: 'center',
    alignItems: 'center',
    overflow: 'hidden',
}