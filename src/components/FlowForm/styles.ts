import { CSSProperties } from "react";

export const container: CSSProperties = {
    display: "grid",
    top: 0,
    bottom: 0,
    right: 0,
    left: 0,
    overflow: "hidden",
    margin: "auto",
};
